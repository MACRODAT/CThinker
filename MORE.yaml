I have an app in d:\ai\Cthinker.
I have implemented a tools workshop system which didn't go well.
My idea:
I want a complete function prompt executor.
So I've got this parser: 
async def resolve_placeholders(self, text: str, db: Session, agent, last_quest) -> str at line 1168 in engine.py
That parses nested conditionals and simple variables and boolean variables.
IT WORKS WELL. IT PARSES THOSE VARIABLES CORRECTLY.
But the functions/tools do NOT work.
But I need to implement:
- functions without args, format: 
*/
func
/*
- functions with arguments, format: 
*/
func
arg1
arg2
/*
- other custom_tools with/without args, format: 
*/
custom_tool
arg1
/*
- Also, when a paid external tool is called, you must pay its owner. (may be implemented)

Also, why call an LLM in execute_custom_tool? => DO NOT! PARSE the prompt in code.
Basically, use resolve_placeholders, which should help you with conditionals and simple variables. Catch: some variables may be inside functions and some functions may be inside conditionals.
So, resolve_placeholders calls execute_custom_tool and vice versa until everything is resolved.

So I want you to go over all functions that handle custom tools:
- starts from :
async def invoke_tool(tool_id: str, req: schemas.ToolInvokeRequest, db: Session = Depends(database.get_db)) in main.py line 737
- calls 
async def _execute_inline_command(self, cmd: str, args: list, db: Session, agent: Agent) -> str in engine.py line 1147
- ....
- The more you leave resolve_placeholders intact, the better. 
Correct all these to handle the prompt below (as example):

--------- PROMPT CODE ----------
{{
    pending_quests_exist    
        Mission:
        Turn ideas into structured artifacts that can justify the cost of threads and lead to approval rewards.
        You are allowed to be creative here — but only in ways that can be **finished**, **used**, and **approved**.
        We may call tools and do nested conditionals.
        When you arrive here, it should call automatically this function and put in the result. File created: *\/CREATE_FILE|content.txt|More content on your hard drive!\/*
        When you get in here, it should call an external tool. Current time: *\/get_time\/*
        I could also call another external paid tool with arguments like so. Result: *\/external_paid_tool|arg1|arg2\/*
        I may also produce complex stuff like:
        */
        READ_FILE
        file.txt
        /*
        OR even:
        */
        HTTP_POST
          */
          READ_FILE
          url.txt
          /*
        /*
        Basically, the parser needs to be perfect to handle every single prompt!
        {{
            pending_invitations_exist
                Returns this sentence with the name of the agent which is {agent}
                Now we'll send it to server. Result:
                */
                HTTP_POST
                http://localhost:98327/seen/{agent}
                /*
            /ELSE/
                Returns another sentence with the name of the agent which is {agent}
        }}
    /ELSE/
        No mission for today
        Rest!
}}



STILL DOES NOT WORK CORRECTLY.
THE PROMPT PARSER FUNCTION WORKS WELL. BUT THE WORKSHOP PROMPT IS NOT PARSER CORRECTLY!
- CONDITIONALS ARE NOT PARSED! (BAD)
- SIMPLE VARIABLES ARE PARSED. (GOOD)
- FUNCTIONS AND external TOOLS DO NOT WORK (BAD)
*/
get_time
/*
